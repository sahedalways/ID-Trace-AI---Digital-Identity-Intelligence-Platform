RewriteEngine On

# If the requested filename exists (with or without .php), serve it directly

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Map extensionless URLs to their .php counterparts

RewriteRule ^(.+)$ $1.php [L]
